import {Text, SafeAreaView} from 'react-native';
import React from 'react';
import Layout from './Layout';

const App = () => {
  return (
    <SafeAreaView>
      <Layout />
    </SafeAreaView>
  );
};

export default App;
